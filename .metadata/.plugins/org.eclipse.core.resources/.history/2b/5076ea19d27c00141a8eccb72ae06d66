
Ti.UI.setBackgroundColor('#DEF5FF');

//JSON
var advertisement = [
	{title: "Ad Campaign"},
	{title: "Certified Partners"},
	{title: "Manual vs Automated"},
	{title: "Data Mining"},
	{title: "Strategy"},
	{title: "Media Buying", },
	{title: "Analytics Dashboard"}];

var window1 = Ti.UI.createWindow({
	backgroundColor: "#F4F7FF"
});

var titleView = Ti.UI.createView({
	height: 65,
	backgroundColor: "#BFBFBF"
});

var border = Ti.UI.createView({
	height: 1,
	backgroundColor: "#FFFFFF",
	top: titleView.height + titleView.top
});

var mainLabel = Ti.UI.createLabel({
	text: "Online Advertising Terms",
	font: {fontSize: 20, fontFamily: "Arial", fontWeight: "bold"},
	top: 30,
	width: '100%',
	textAlign: "center"
});

var adTerms = Ti.UI.createTableView({
	top: border.top + border.height
});

if(Ti.Platform.name === "iPhone OS"){
	adTerms.style = Ti.UI.iPhone.TableViewStyle.GROUPED;
}

var adSection = Ti.UI.createTableViewSection({
	headerTitle: "What should go here",
	footerTitle: "And the footer says?"
});
var getDetail = function(){
	var detailWindow = Ti.UI.createWindow({
		backgroundColor: "#f5f5f5"
});
	var detailTitleView = Ti.UI.createView({
		height: 65,
		backgroundColor: "#BFBFBF",
		top: 0,
});
	
	var detailBorder = Ti.UI.createView({
		backgroundColor: "#FFFFFF",
		height: 1,
		top: detailTitleView.height + detailTitleView.top
});
	
	var detailLabel = Ti.UI.createLabel({
		text: this.title, 
		font: {fontSize: 20, fontFamily: "Arial", fontWeight: "bold" },
		top: 30,
		width: '100%',
		textAlign: "center"
});

	detailTitleView.add(detailLabel);
	detailWindow.add(detailTitleView, detailBorder);
	
	detailWindow.open();
	
for(var i = 0, j = advertisement.length; i < j; i+= 1){
	var theRow = Ti.UI.createTableViewRow({
		title: advertisement[i].title,
		haschild: true
});



	if(Ti.UI.Platform.name === "iPhone OS"){
		theRow.hasChild = false;
		theRow.hasDetail = true;
};

adTerms.add(theRow);
theRow.addEventListener("click", getDetail);  




adTerms.setData(advertisement);

titleView.add(mainLabel);
window1.add(titleView, border, advertisement);
window1.open(); 
